worker_processes 4;

events { worker_connections 1024; }

http {
        upstream container {
              least_conn;
              server localhost:7666;
              server localhost:7667;
              server localhost:7668;
        }
        server {
              listen 80;
              location / {
                  proxy_pass http://localhost;
              }
        }
}